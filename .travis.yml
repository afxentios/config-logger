language: python
sudo: false
python:
  - "2.7"

branches:
    only:
    - master

install:
  - "pip install pytest-cov coveralls config_manager"

script:
  - "py.test --cov-config .coveragerc  --cov config_logger"

after_success:
  - "coveralls"